name: Build and Deploy
on:
  push:
  workflow_dispatch:
env:
  IMAGE_NAME: ghcr.io/ghetto05/ghettogpt
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    # runs-on: self-hosted

    permissions:
      contents: read
      packages: write
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Show main.py contents
      run: cat ./Main.py

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - name: Build and push Docker image
      run: |
        # Construct tag with build number
        if [[ "${GITHUB_REF}" == "refs/heads/master" ]]; then
          BASE_TAG="latest"
        else
          BASE_TAG="dev"
        fi

        TAG="${BASE_TAG}-build-${GITHUB_RUN_NUMBER}"

        echo "Tagging image as: $TAG"

        docker build --no-cache -t $IMAGE_NAME:$TAG .
        docker push $IMAGE_NAME:$TAG

        # Push 'latest' tag only for master branch (optional)
        if [[ "$BASE_TAG" == "latest" ]]; then
          docker tag $IMAGE_NAME:$TAG $IMAGE_NAME:latest
          docker push $IMAGE_NAME:latest
        fi

        # Push 'dev' tag only for non-master branch (optional)
        if [[ "$BASE_TAG" == "dev" ]]; then
          docker tag $IMAGE_NAME:$TAG $IMAGE_NAME:dev
          docker push $IMAGE_NAME:dev
        fi
    - name: Call Portainer webhook to trigger prod deploy
      if: success() && github.ref == 'refs/heads/master'
      run: |
        curl -X POST https://portainer.ghetto05.com/api/stacks/webhooks/576b59cc-8544-4804-a936-e817c711434e # prod instance
    - name: Call Portainer webhook to trigger dev deploy
      if: success() && github.ref != 'refs/heads/master'
      run: |
        curl -X POST https://portainer.ghetto05.com/api/stacks/webhooks/bb5093a0-2769-4ed4-a4de-bfc3d4d63c8f # dev instance
